server:
  port: 6060

ledger:
  web:
    relays:
      - wss://relay.imani.casa
    timeout: 30s
    cache-ttl: 30s
    api-base: https://staging.imani.casa/api/v1
    storage:
      enabled: false
      path: ${user.home}/.cashu-ledger/ndb
      max-size-bytes: 536870912
      event-ttl: 30d

spring:
  thymeleaf:
    cache: false

  security:
    user:
      name: disabled
      password: disabled
